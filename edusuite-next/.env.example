# MySQL connection (same database the .NET app used)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=edusuite_db

# Secret used to sign session JWTs. Generate with: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-long-random-string
